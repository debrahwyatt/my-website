import { useState } from "react";

const Magic = (bannerText2?: string) => {
    // const [bannerText, setBannerText] = useState("Welcome!");
    // if(bannerText2) setBannerText(bannerText2)
    // console.log(bannerText2)
    return bannerText2
}

export default Magic;
